# ---- Local MySQL (running on your own machine, e.g. via XAMPP/MySQL Workbench) ----
spring.datasource.url=jdbc:mysql://localhost:3306/fund_raiser
spring.datasource.username=root
spring.datasource.password=Mohamed@12oct
# ---- Schema auto-update is fine for local dev ----
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

server.port=8080

# ---- Local upload folder (relative path, works on your machine) ----
app.upload.dir=./uploads

# ---- Admin login for local testing only ----
app.admin.username=sameerkaleemi@gmail.com
app.admin.password=1230